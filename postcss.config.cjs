/** @type {import('postcss-load-config').Config} */
const config = {
  // eslint-disable-next-line no-undef
  plugins: [require("css-declaration-sorter")],
};

// eslint-disable-next-line no-undef
module.exports = config;
